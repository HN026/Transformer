I suggest to install PyTorch in a containerised environment.

install PyTorch
pip3 install torch torchvision torchaudio /// for pip
conda install pytorch torchvision torchaudio pytorch-cuda=11.7 -c pytorch -c nvidia
